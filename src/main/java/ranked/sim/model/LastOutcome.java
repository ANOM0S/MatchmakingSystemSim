package ranked.sim.model;

/**
 * Enum reprezentujący ostatni wynik gracza w meczu.
 * WIN — Gracz wygrał ostatni mecz.
 * LOSS — Gracz przegrał ostatni mecz.
 * NONE — Gracz nie brał udziału w ostatnim meczu lub wynik jest nieznany.
 */

public enum LastOutcome {
    WIN,
    LOSS,
    NONE
}
